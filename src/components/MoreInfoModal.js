import React from 'react';
import styled from 'styled-components';

const MoreInfoModal = () => {
  // if (!isLogin)

  return (
    <React.Fragment>
      <div>더보기 클릭했을 때 모달</div>
      <div>로그인안하고 더보기 클릭했을 때 모달</div>
    </React.Fragment>
  );
};

export default MoreInfoModal;
